{
  "mcpServers": {
    "memclaw": {
      "type": "http",
      "url": "https://memclaw.dev/mcp/",
      "headers": {
        "Authorization": "Bearer ${user_config.MEMCLAW_API_KEY}"
      }
    }
  }
}
